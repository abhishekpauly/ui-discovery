# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Engine output (crawl snapshots + screenshots)
output/

# Saved login sessions — treat like passwords, never commit
session.json
*.session.json

# Real, target-specific scope configs. A config naming an actual internal
# host is reconnaissance; the sanitised example lives in
# examples/authenticated-spa.scope.yaml.
*.local.yaml
*.local.json


# OS
.DS_Store
Thumbs.db
